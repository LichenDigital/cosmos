/**
 * Cosmos core executes modules. Modules can be written and executed in different languages using the common protocol:
 * @param {string} module - Module to be executed. Can either be name alias or identifier ID (default)
 * @param {string} data - Data to input into the module
 */
export default cosmos = (module, data) => {
    /* MODULE */   
}


// module, m = core method used to build modules. Provides registration, scaforlding, and access to cosmos methods

// queue, q = core method for managing queues

// connection, c = core method for managing connections (inputs & outputs)

// file, f = core method for managing files

// ui, i = core method for managing user interfaces